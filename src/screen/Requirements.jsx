import React from 'react';
import { View, Text } from 'react-native';

const Requirements = () => {
    return (
        <View>
            <Text>

            Requerimientos
            </Text>
        </View>
    );
};

export default Requirements;