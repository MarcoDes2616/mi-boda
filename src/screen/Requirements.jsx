import React from 'react';
import { View } from 'react-native';

const Requirements = () => {
    return (
        <View>
            Requerimientos
        </View>
    );
};

export default Requirements;